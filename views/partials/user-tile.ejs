<!DOCTYPE html>
<%/*
	Expects variables:
		user - the user object to display
		check_if_friends - if true, the tile will send a request to determine if
							the auth'd user is friends, and if not will display an
							"Add friend" button
*/%>
<html>
<head>
	<%- partial('partials/resources.ejs', {
			stylesheets: [
				'/public/lib/bootstrap/css/bootstrap.min.css',
				'/public/gf-style.css',
				'/public/user-tile.css'
			],
			scripts: []
		}) %>
</head>
<body>
	<div class="user-tile-partial">
		<div class="user-tile alert alert-info">
			<div class="pull-left">
				<img src="/api/users/picture/get?username=<%= user.username %>?type=normal">
			</div>
			<div class="user-info">
				<strong><%= user.name.full %></strong>
				<div><%= user.username %></div>
			</div>
		</div>
	</div>
</body>
</html>
