<!DOCTYPE html>
<%/*
	Expects variables:
		groups - an array of groups objects to populate the initial list
					(it is expected that this is the start of the group list)
		offset - the offset of the current view
		maxcount - the maxcount of the current view
*/%>
<html>
<head>
	<%- partial('partials/resources.ejs', {
			stylesheets: [
				'/public/lib/bootstrap/css/bootstrap.min.css',
				'/public/gf-style.css',
				'/public/group-list/group-list.css'
			],
			scripts: [
				'/public/lib/jquery/jquery.min.js',
				'/public/group-list/group-list.js'
			]
		}) %>
</head>
<body>
	<div class="group-invitation-list-partial">
		<%	if (groups.length == 0) { %>
			<div class="alert alert-warning">
				<div>You aren't in any groups yet.
					 Add more <a href="/friends/search">friends</a>!</div>
			</div>
		<% } else { %>
			<%  for (var i in groups) { %>
				<div class="group-tile-outer">
					<%- partial('partials/group-tile.ejs', {
							group: groups[i],
							alert_class: "alert-info"
						}) %>
				</div>
			<% } %>
			<% if (groups.length == maxcount) { %>
				<!-- TODO: load more groups feature -->
			<% } %>
		<% } %>
	</div>
</body>
</html>
