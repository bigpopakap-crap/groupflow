<!DOCTYPE html>
<%/*
	Expects variables:
		active - the NAME link to be active
		title - the title of the navigation
		items - an array of objects:
			{ name: <name of link>, href: <the href> }
			or { header: <text> }
*/%>
<html>
<head>
	<%- partial('partials/resources.ejs', {
			stylesheets: [
				'/public/lib/bootstrap/css/bootstrap.min.css',
				'/public/gf-style.css',
				'/public/sidebar.css'
			],
			scripts: []
		}) %>
</head>
<body>
	<div class="sidebar-partial">
		<div class="sidebar-partial-inner">
			<h3 class="title"><%= title %></h3>
			<ul class="nav nav-list">
				<% for (var i=0; i<items.length; i++) { %>
					<% var item = items[i]; %>
					<% if (item.header) { %>
						<li class="nav-header"><%= item.header %></li>
					<% } else { %>
						<li <% if (active === item.name) { %>class="active"<% } %>>
							<a href="<%= item.href %>"><%= item.name %></a>
						</li>
					<% } /* end if */
				} /* end for */ %>
			</ul>
		</div>
	</div>
</body>
</html>
